version: 2
updates:
  # The version catalog and plugin versions.
  - package-ecosystem: gradle
    directory: /
    schedule:
      interval: weekly

  # Action versions in workflows and the setup composite.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
